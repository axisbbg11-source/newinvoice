build:
  language: python
  buildCommand: pip install -r backend/requirements.txt
  runCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT

start:
  cmd: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT